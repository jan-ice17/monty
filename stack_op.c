#include "monty.h"

/**
 * jan_stack - Sets the stack mode to LIFO (Last In, First Out).
 * @head: Double pointer to the head of the stack (unused).
 * @counter: Line number
 */
void jan_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}

