#include "monty.h"

/**
 * jan_nop - Does nothing.
 * @head: Double pointer to the head of the stack.
 * @counter: Line number in the Monty file where the nop operation is called.
 */
void jan_nop(stack_t **head, unsigned int counter)
{
	(void)counter;
	(void)head;
}

